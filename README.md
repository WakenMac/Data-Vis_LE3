How to run me?

1. Clone the repository
2. Run the following lines of code to set up the virtual environment:

python -m venv .venv
.venv\Scripts\activate
python -m pip install -r requirements.txt

3. Run the code below to start the server

shiny run --reload app.py

4. Enjoy :>
